<Window x:Class="MapCityProject.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MapCityProject.Windows"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        <Border Background="Coral" CornerRadius="10" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <StackPanel>
                <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBlock Text="Username:" FontSize="16" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameTextBox" FontSize="16" Margin="0,0,0,10"/>
                <TextBlock Text="Password:" FontSize="16" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" FontSize="16" Margin="0,0,0,20"/>
                <Button Content="Login" Width="100" Height="30" HorizontalAlignment="Center" Click="LoginCheckUser" Foreground="GreenYellow" Background="green"/>
            </StackPanel> 
        </Border>
    </Grid>
</Window>
